# JitPack configuration file
# This tells JitPack how to build your Android library

jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.7-tem
  - sdk use java 17.0.7-tem

install:
  - ./gradlew clean build publishToMavenLocal -x test --stacktrace

